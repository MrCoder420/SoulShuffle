const fs = require('fs'); let content = fs.readFileSync('services/cardService.ts', 'utf8'); const lines = content.replace(/\0/g, '').split('\n'); lines[78] = 'export const fetchUserDeck = async (): Promise<any[]> => { const response = await api.get(' + String.fromCharCode(39) + '/user/deck' + String.fromCharCode(39) + '); return response.data.data.cards; };'; fs.writeFileSync('services/cardService.ts', lines.join('\n'), 'utf8');
